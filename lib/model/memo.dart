class Memo {
  String title;
  String detail;
  DateTime createdTime;
  DateTime updateDate;

  Memo(
      {required this.title,
      required this.detail,
      required this.createdTime,
      required this.updateDate});
}
